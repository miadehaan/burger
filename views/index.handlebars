{{!--  * Setup the `index.handlebars` to have the template that Handlebars can render onto.
    * Create a button in `index.handlebars` that will submit the user input into the database.
--}}

<h1>Burgers!</h1>

<img src="../public/assets/images/burger-icon.png" id="burgerIcon" alt="burger icon">

<h2>Remaining Burgers</h2>
{{!-- Display list of burgers that have NOT been devoured --}}
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured Burgers</h2>

{{!-- Display list of burgers that HAVE been devoured --}}
<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Not eaten yet<br>
    <input type="radio" name="devoured" value="0"> I've already eaten it...
  </div>

  <button type="submit">Add Burger!</button>
</form>
